{
  "name": "@petriflow/engine",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "build": "bun build src/index.ts --outdir dist --target node",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "petri-ts": "^0.1.5"
  },
  "devDependencies": {
    "typescript": "^5.7",
    "@types/bun": "^1"
  }
}
